import React from 'react'

import './Loader.css'

const Loader = props => {
        return (

            <div className="Loader"><span className={"page"}> </span></div>
        )
};

export default Loader